# Pinned to known-stable versions as of late 2024 / early 2025. The
# previous `>=` floor let `--build` pull whatever was current at build
# time, which is how we ended up with a uvicorn that silently dropped
# device POSTs to /q/ after the 2026-05-05 rebuild. Pin until we have
# a reason to bump.
fastapi==0.115.0
uvicorn==0.30.6
starlette==0.38.6
pydantic==2.9.2
redis==5.1.1
msgpack==1.1.0
httpx==0.27.2
pytest==8.3.3
# Google OAuth (v1.5 — see fr_v15_auth.md). google-auth validates
# ID tokens against Google's JWKS; requests is the transitive dep
# the token-exchange call uses.
google-auth==2.35.0
requests==2.32.3
